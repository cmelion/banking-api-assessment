node_modules
# Coverage reports should not be committed - generated in CI only
coverage/
# Keep environment variables out of version control
.env
.env.*
# Keep dist folder out of version control
dist
# Test databases
*.db
*.db-journal

# Claude Code settings (may contain sensitive credentials)
.claude/

# Any files with credentials
*credentials*
*secrets*



